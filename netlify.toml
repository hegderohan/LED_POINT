# Netlify build & hosting config.
# Connect this repo in the Netlify dashboard; these settings are picked up automatically.
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
